# Верес-Тариф - Система управления тарифами

Современная система для управления тарифами и расчётов стоимости доставки грузов согласно техническому заданию.

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.8+
- Node.js 16+
- Docker (опционально)
- PostgreSQL (для продакшн) или SQLite (для разработки)
- Tesseract OCR (для обработки сканированных документов)

### Установка Tesseract OCR

**Windows:**
- Скачайте установщик с [GitHub](https://github.com/UB-Mannheim/tesseract/wiki)
- Установите в `C:\Program Files\Tesseract-OCR\`
- Добавьте в PATH: `C:\Program Files\Tesseract-OCR\`

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
```

**macOS:**
```bash
brew install tesseract tesseract-lang
```

### Установка и запуск

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd logistics_app
```

2. **Установите зависимости**
```bash
# Backend
pip install -r requirements.txt

# Frontend
cd frontend
npm install
cd ..
```

3. **Инициализируйте базу данных и создайте пользователей**
```bash
python -c "
from backend.database import engine
from backend.models import Base
Base.metadata.create_all(bind=engine)

# Создание демо-пользователей
from backend.services.security import get_password_hash
from backend.models import User
from sqlalchemy.orm import Session
from backend.database import SessionLocal

db = SessionLocal()

# Создаем пользователей если их нет
if not db.query(User).filter(User.username == 'admin').first():
    admin_user = User(
        username='admin',
        email='admin@veres.ru',
        hashed_password=get_password_hash('admin123'),
        role='admin',
        is_active=True
    )
    db.add(admin_user)

if not db.query(User).filter(User.username == 'client').first():
    client_user = User(
        username='client',
        email='client@veres.ru', 
        hashed_password=get_password_hash('client123'),
        role='client',
        is_active=True
    )
    db.add(client_user)

if not db.query(User).filter(User.username == 'forwarder').first():
    forwarder_user = User(
        username='forwarder',
        email='forwarder@veres.ru',
        hashed_password=get_password_hash('forwarder123'),
        role='forwarder',
        is_active=True
    )
    db.add(forwarder_user)

db.commit()
db.close()
print('Демо-пользователи созданы успешно!')
"
```

4. **Запустите приложение**

**Вариант 1: Локальный запуск (SQLite)**
```bash
# Backend (в одном терминале)
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Frontend (в другом терминале)
cd frontend
npm run dev
```

**Вариант 2: Docker Compose (PostgreSQL)**
```bash
docker-compose up --build
```

**Вариант 3: Миграция в PostgreSQL**
```bash
# Сначала запустите PostgreSQL
docker-compose up db

# Обновите переменную окружения
export DATABASE_URL="postgresql+psycopg2://postgres:postgres@localhost:5432/veres"

# Запустите приложение
cd backend && uvicorn main:app --reload
```

5. **Откройте приложение**
- Frontend: http://localhost:5173
- Backend API: http://localhost:8000
- API документация: http://localhost:8000/docs

## 👤 Демо-пользователи

После выполнения команды инициализации будут созданы следующие пользователи:

| Роль | Логин | Пароль | Описание |
|------|-------|--------|----------|
| Администратор | `admin` | `admin123` | Полный доступ к системе |
| Клиент | `client` | `client123` | Основные функции |
| Экспедитор | `forwarder` | `forwarder123` | Расширенные функции |

## 🎯 Основные функции

### ✅ Реализовано согласно ТЗ:

#### Для клиентов:
- 📊 **Калькулятор стоимости доставки** - расчёт стоимости по различным маршрутам
- 📜 **История запросов** - просмотр предыдущих расчётов
- 📄 **Генерация КП** - автоматическое создание коммерческих предложений в формате DOCX/PDF
- 💱 **Конвертация валют** - автоматическая конвертация по курсам ЦБ РФ
- 🎛️ **Форма приложения** - полное соответствие скриншотам ТЗ

#### Для администраторов:
- 👥 **Управление пользователями** - создание и редактирование пользователей
- 💰 **Управление скидками** - настройка персональных скидок
- 📂 **Загрузка тарифов** - импорт тарифов от поставщиков
- 📑 **Архив тарифов** - управление архивными данными
- 🔧 **Вспомогательные таблицы** - управление СВХ, автовывозом, предперевозкой

#### Для экспедиторов:
- 📤 **Загрузка файлов** - поддержка XLS, XLSX, PDF, DOCX, JPG, PNG
- 🔍 **OCR обработка** - распознавание сканированных документов
- 📋 **Массовые расчёты** - обработка множественных запросов
- 📊 **Аналитика** - статистика и отчёты

#### Системные возможности:
- 🏗️ **Поддержка индивидуальных шаблонов** - для каждого подрядчика
- 🔄 **Распознавание и структурирование данных** - форматы JPEG, Excel, PDF, Word
- 📋 **Структурирование данных** - под единый шаблон
- 💱 **Автоматическая конвертация валют** - в рубли по текущему курсу ЦБ РФ
- 📄 **Экспорт в коммерческое предложение** - PDF/Excel
- 📦 **Архивирование устаревших тарифов**
- 🎯 **Массовый выбор позиций** - для расчёта
- 💰 **Фиксированная или процентная наценка** - по поставщикам
- 🤖 **Интеграция с Telegram-ботом** - поиск и вывод тарифов, загрузка новых ставок
- 👥 **Система ролей** - сотрудник, экспедитор, клиент
- 🎨 **Скидочная система** - для пользователей

### 📋 Формы КП согласно ТЗ:

#### Автомобильная доставка:
- ✅ Автомобильная доставка генерального груза, базис EXW
- ✅ Автомобильная доставка генерального груза, базис FCA

#### Железнодорожная доставка:
- ✅ Железнодорожная доставка генерального груза, базис EXW
- ✅ Железнодорожная доставка генерального груза, базис FCA
- ✅ Железнодорожная доставка генерального груза, базис FOB

#### Мультимодальная доставка:
- ✅ Мультимодальная доставка генерального груза, базис EXW
- ✅ Мультимодальная доставка генерального груза, базис FCA
- ✅ Мультимодальная доставка генерального груза, базис FOB

#### Дополнительные услуги:
- ✅ Автовывоз КТК

### 🔧 Логика таблиц согласно ТЗ:

#### Основные таблицы:
- ✅ Мультимодальные перевозки
- ✅ Автомобильные перевозки
- ✅ Морские перевозки
- ✅ Железнодорожные перевозки

#### Вспомогательные таблицы:
- ✅ Автовывоз (с формулой: `база + за_км * км_от_свх`)
- ✅ СВХ (склады временного хранения)
- ✅ Прекерридж ЖД (с формулой: `база + за_км * км_от_станции + локальные`)
- ✅ Прекерридж море и мультимодальные (с формулой: `база + за_км * км_от_порта + ТНС`)

## 🛠 Технологии

### Backend:
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL/SQLite** - база данных
- **JWT** - аутентификация
- **Pandas** - обработка Excel файлов
- **PDFPlumber** - извлечение текста из PDF
- **Pytesseract** - OCR для сканированных документов
- **Python-docx** - работа с Word документами
- **ReportLab** - генерация PDF документов

### Frontend:
- **React** - пользовательский интерфейс
- **Vite** - сборщик проекта
- **React Router** - маршрутизация
- **Axios** - HTTP клиент

### Интеграции:
- **API ЦБ РФ** - курсы валют
- **Telegram Bot** - мобильный интерфейс

## 📁 Структура проекта

```
logistics_app/
├── backend/                 # Backend приложение
│   ├── routers/            # API маршруты
│   │   ├── auth.py         # Аутентификация
│   │   ├── users.py        # Управление пользователями
│   │   ├── tariffs.py      # Тарифы и архив
│   │   ├── files.py        # Загрузка файлов
│   │   ├── quotes.py       # Калькулятор
│   │   ├── offers.py       # Коммерческие предложения
│   │   ├── currency.py     # Курсы валют
│   │   ├── reference.py    # Вспомогательные таблицы
│   │   └── discounts.py    # Скидки
│   ├── services/           # Бизнес-логика
│   │   ├── security.py     # Аутентификация
│   │   ├── parsers.py      # Парсинг файлов
│   │   ├── cbr.py          # Курсы валют ЦБ РФ
│   │   └── documents.py    # Генерация документов
│   ├── models.py           # Модели данных
│   ├── schemas.py          # Pydantic схемы
│   └── main.py             # Точка входа
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── main.jsx        # Основной компонент
│   │   └── styles.css      # Стили
│   └── package.json
├── telegram_bot/           # Telegram бот
└── docker-compose.yml      # Docker конфигурация
```

## 🔧 Конфигурация

### Переменные окружения:
```bash
# Backend
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
DATABASE_URL=sqlite:///./veres.db  # или postgresql+psycopg2://...
SECRET_KEY=your-secret-key

# Frontend
VITE_API=http://localhost:8000

# Telegram Bot
BOT_TOKEN=your-bot-token
API_BASE=http://localhost:8000
```

### Настройка OCR (Tesseract):
```bash
# Windows
# Скачайте и установите Tesseract с https://github.com/UB-Mannheim/tesseract/wiki

# Linux
sudo apt-get install tesseract-ocr tesseract-ocr-rus

# macOS
brew install tesseract tesseract-lang
```

## 📝 API документация

После запуска backend сервера, документация API доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Основные эндпоинты:
- `POST /auth/login` - вход в систему
- `GET /auth/me` - информация о пользователе
- `POST /tariffs/upload` - загрузка тарифов
- `POST /calculate/calculate` - расчёт стоимости
- `POST /offers/generate` - генерация КП
- `GET /currency/` - курсы валют
- `GET /reference/svh` - справочник СВХ
- `GET /reference/trucking` - справочник автовывоза
- `GET /reference/precarriage/rail` - справочник предперевозки ЖД
- `GET /reference/precarriage/sea` - справочник предперевозки море

## 🤖 Telegram Bot

Бот поддерживает следующие команды:
- `/start` - приветствие и справка
- `/help` - подробная справка
- `/find` - поиск тарифов
- `/calculate` - расчёт стоимости доставки
- `/getkp` - генерация коммерческого предложения
- `/upload` - загрузка файлов с тарифами
- `/status` - проверка статуса сервера

### Форматы данных для бота:
```
Поиск тарифов:
тип_транспорта, базис, город_отправления, город_назначения

Расчёт стоимости:
тип_транспорта, базис, город_отправления, город_назначения, вес_кг, объём_м³, наименование_груза
```

## 🤝 Разработка

### Добавление новых функций:
1. Создайте модель в `backend/models.py`
2. Добавьте схему в `backend/schemas.py`
3. Создайте роутер в `backend/routers/`
4. Добавьте компонент в `frontend/src/main.jsx`

### Стили:
- Используйте CSS переменные из `frontend/src/styles.css`
- Следуйте существующему дизайн-системе
- Добавляйте адаптивность для мобильных устройств

### Тестирование:
```bash
# Запуск тестов
pytest

# Проверка кода
flake8 backend/
black backend/
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи backend сервера
3. Убедитесь, что все зависимости установлены
4. Проверьте, что база данных инициализирована
5. Убедитесь, что Tesseract OCR установлен (для обработки сканов)

## 📄 Лицензия

Проект разработан для внутреннего использования компании Верес.
