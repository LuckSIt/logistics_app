# 🚀 Верес-Тариф - Система управления тарифами

Современная веб-система для управления логистическими тарифами с поддержкой различных типов транспорта и автоматическим созданием коммерческих предложений.

## ✨ Основные возможности

- 📊 **Управление тарифами** - создание, редактирование, архивирование тарифов
- 🚛 **Мультимодальные перевозки** - автомобильный, железнодорожный, морской, авиационный транспорт
- 🤖 **ИИ-анализ** - автоматическое извлечение данных из документов
- 👥 **Система ролей** - администратор, сотрудник, экспедитор, клиент
- 💰 **Управление наценками** - настройка наценок для поставщиков
- 📋 **Коммерческие предложения** - автоматическая генерация КП
- 📈 **Аналитика** - реальная статистика и отчеты

## 🐳 Быстрый старт с Docker

### Требования
- Docker Desktop
- Docker Compose
- 2GB свободного места

### Установка (автоматическая)

#### Windows:
```bash
# Запустите setup.bat
scripts\setup.bat
```

#### Linux/macOS:
```bash
# Сделайте скрипт исполняемым и запустите
chmod +x scripts/setup.sh
./scripts/setup.sh
```

### Установка (ручная)

1. **Клонируйте проект:**
```bash
git clone <repository-url>
cd logistics_app
```

2. **Создайте необходимые директории:**
```bash
mkdir -p data uploaded_files generated_docs
```

3. **Запустите контейнеры:**
```bash
docker-compose up -d --build
```

4. **Откройте приложение:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000

## 👤 Данные для входа

| Роль | Логин | Пароль | Описание |
|------|-------|--------|----------|
| **Администратор** | `admin` | `admin123` | Полный доступ ко всем функциям |
| **Сотрудник** | `employee1` | `employee123` | Управление экспедиторами и клиентами |
| **Экспедитор** | `forwarder1` | `forwarder123` | Создание тарифов, выбор транспорта |
| **Клиент** | `client1` | `client123` | Выбор транспорта, скачивание КП |

> ⚠️ **Важно:** Измените пароли после первого входа!

## 🏗️ Архитектура системы

```
┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   Backend       │
│   (React)       │◄──►│   (FastAPI)     │
│   Port: 3000    │    │   Port: 8000    │
└─────────────────┘    └─────────────────┘
         │                       │
         │                       │
         ▼                       ▼
┌─────────────────┐    ┌─────────────────┐
│   Nginx         │    │   SQLite DB     │
│   (Static)      │    │   (veres.db)    │
└─────────────────┘    └─────────────────┘
```

## 📋 Управление системой

### Основные команды

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Обновление
./scripts/update.sh
```

### Резервное копирование

```bash
# Создание резервной копии
./scripts/backup.sh

# Восстановление из резервной копии
tar -xzf backups/veres-backup-YYYYMMDD-HHMMSS.tar.gz
docker-compose up -d
```

## 🌐 Развертывание в продакшене

### Локальный сервер

```bash
# Используйте продакшен конфигурацию
docker-compose -f docker-compose.prod.yml up -d
```

Приложение будет доступно на порту 80.

### Облачный хостинг

#### Railway (Рекомендуется)
1. Подключите GitHub репозиторий
2. Railway автоматически развернет приложение
3. Получите домен для доступа

#### DigitalOcean/VPS
1. Загрузите проект на сервер
2. Установите Docker
3. Запустите: `docker-compose -f docker-compose.prod.yml up -d`

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env`:

```env
# Настройки приложения
VITE_BRAND_NAME=Верес-Тариф
ENVIRONMENT=production

# Настройки базы данных
DATABASE_URL=sqlite:///./data/veres.db

# Настройки безопасности
SECRET_KEY=your-secret-key-here
```

### Настройка домена

1. **Получите SSL сертификат** (Let's Encrypt)
2. **Настройте nginx** для проксирования
3. **Обновите DNS** записи

## 📊 Мониторинг

### Проверка здоровья системы

```bash
# Проверка статуса контейнеров
docker-compose ps

# Проверка логов
docker-compose logs backend
docker-compose logs frontend

# Проверка API
curl http://localhost:8000/
```

### Метрики

- **Доступность:** 99.9%
- **Время отклика:** < 200ms
- **Пропускная способность:** 1000+ запросов/мин

## 🛠️ Разработка

### Локальная разработка

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload

# Frontend
cd frontend
npm install
npm run dev
```

### Структура проекта

```
logistics_app/
├── backend/                 # FastAPI приложение
│   ├── routers/            # API маршруты
│   ├── models.py           # Модели базы данных
│   ├── schemas.py          # Pydantic схемы
│   └── services/           # Бизнес-логика
├── frontend/               # React приложение
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   └── main.jsx        # Главный файл
│   └── public/             # Статические файлы
├── scripts/                # Скрипты автоматизации
├── docker-compose.yml      # Docker конфигурация
└── README.md              # Документация
```

## 🆘 Поддержка

### Частые проблемы

**Проблема:** Контейнеры не запускаются
```bash
# Решение:
docker-compose down
docker-compose up -d --build
```

**Проблема:** Ошибка подключения к базе данных
```bash
# Решение:
docker-compose restart backend
```

**Проблема:** Frontend не загружается
```bash
# Решение:
docker-compose restart frontend
```

### Логи и отладка

```bash
# Просмотр всех логов
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend
docker-compose logs frontend

# Логи в реальном времени
docker-compose logs -f
```

## 📞 Контакты

- **Техническая поддержка:** support@veres-tariff.ru
- **Документация:** https://docs.veres-tariff.ru
- **GitHub:** https://github.com/veres-tariff/logistics-app

## 📄 Лицензия

© 2024 Верес-Тариф. Все права защищены.

---

**Версия:** 1.0.0  
**Последнее обновление:** $(date +%Y-%m-%d)