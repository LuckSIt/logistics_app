# 💰 Выбор плана на Render для Верес-Тариф

## Сравнение планов

| Характеристика | Free | Starter | Standard |
|---------------|------|---------|----------|
| **Стоимость** | $0 | $7/месяц | $25/месяц |
| **Persistent Storage** | ❌ Нет | ✅ Да | ✅ Да |
| **Засыпание сервиса** | ⚠️ Да (15 мин) | ✅ Нет | ✅ Нет |
| **RAM** | 512 MB | 512 MB | 2 GB |
| **Build Minutes** | 400/мес | 400/мес | 400/мес |
| **Bandwidth** | 100 GB | 100 GB | 100 GB |

---

## 🎯 Рекомендации по выбору

### Free Tier - Для демонстрации и тестирования

**Используйте если:**
- ✅ Нужно протестировать функционал
- ✅ Демонстрация приложения потенциальным клиентам
- ✅ Учебный проект / портфолио
- ✅ Не критично засыпание сервиса
- ✅ Не важна сохранность данных между обновлениями

**Ограничения:**
- ⚠️ **Данные БД удаляются при каждом деплое!**
- ⚠️ Сервис засыпает после 15 минут неактивности
- ⚠️ Холодный старт ~30-60 секунд
- ⚠️ Нельзя добавить persistent disk

**Конфигурация:** Используйте `render.yaml`

---

### Starter ($7/мес) - Для малого бизнеса

**Используйте если:**
- ✅ Нужна сохранность данных между обновлениями
- ✅ Небольшая команда (до 10 пользователей)
- ✅ Сервис должен быть всегда доступен
- ✅ Важна стабильность работы
- ✅ Умеренная нагрузка

**Преимущества:**
- ✅ Persistent disk для SQLite БД
- ✅ Сервис всегда активен (без засыпания)
- ✅ Быстрый отклик
- ✅ Подходит для продакшн использования

**Конфигурация:** Используйте `render-paid.yaml`

**Стоимость:**
- Backend (Starter): $7/мес
- Frontend (Static Site): $0
- **Итого: $7/мес**

---

### PostgreSQL + Starter ($14/мес) - Для серьёзного продакшена

**Используйте если:**
- ✅ Много пользователей (10+)
- ✅ Критически важные данные
- ✅ Нужно резервное копирование
- ✅ Планируется масштабирование
- ✅ Требуется полноценная СУБД

**Преимущества:**
- ✅ PostgreSQL - надёжная БД с поддержкой транзакций
- ✅ Автоматическое резервное копирование
- ✅ Легко масштабируется
- ✅ Поддержка сложных запросов
- ✅ Лучшая производительность при большой нагрузке

**Стоимость:**
- Backend (Starter): $7/мес
- PostgreSQL Database: $7/мес
- Frontend (Static Site): $0
- **Итого: $14/мес**

---

## 🚀 Как начать

### Вариант 1: Free Tier (тестирование)

```bash
# 1. Используйте render.yaml (уже готов)
# 2. В Render Dashboard: New + → Blueprint
# 3. Подключите репозиторий
# 4. Deploy!
```

**Конфигурация:** `render.yaml` уже настроен для Free Tier

---

### Вариант 2: Starter Plan (продакшн с SQLite)

```bash
# 1. Используйте render-paid.yaml
# 2. Переименуйте или используйте напрямую:
mv render-paid.yaml render.yaml
# 3. В Render Dashboard: New + → Blueprint
# 4. Выберите план Starter при создании
```

**Или через UI:**
1. Создайте сервис с Free tier
2. После создания: Settings → Instance Type → Starter
3. Добавьте Disk: Disks → Add Disk

---

### Вариант 3: PostgreSQL (лучший для продакшена)

#### Шаг 1: Создайте PostgreSQL базу

1. Render Dashboard → **New +** → **PostgreSQL**
2. Настройки:
   - Name: `veres-postgres`
   - Database: `veres_db`
   - User: `veres_user`
   - Region: Frankfurt (тот же что backend)
   - Plan: **Starter** ($7/мес)
3. Создайте базу

#### Шаг 2: Обновите Backend

В `backend/requirements.txt` добавьте:
```txt
psycopg2-binary==2.9.9
```

В `backend/database.py` обновите:
```python
import os
from sqlalchemy import create_engine

# Поддержка и PostgreSQL и SQLite
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "sqlite:///./data/veres.db"
)

# Render предоставляет postgres:// но SQLAlchemy требует postgresql://
if DATABASE_URL.startswith("postgres://"):
    DATABASE_URL = DATABASE_URL.replace("postgres://", "postgresql://", 1)

engine = create_engine(DATABASE_URL)
```

#### Шаг 3: Настройте Environment Variable

В Render Backend Service:
- Settings → Environment
- Найдите `DATABASE_URL`
- Измените на: Подключите к PostgreSQL базе
  - Или вручную: скопируйте Internal Database URL из PostgreSQL сервиса

#### Шаг 4: Deploy

```bash
git add .
git commit -m "Add PostgreSQL support"
git push origin main
```

---

## 📊 Какой план выбрать?

```
┌─────────────────────────────────────────────────────┐
│ Нужны ли вам сохраняемые данные?                   │
└─────────────────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
       Нет                     Да
        │                       │
   ┌────▼────┐          ┌───────▼────────┐
   │  FREE   │          │ Сколько юзеров?│
   │  $0/мес │          └───────┬────────┘
   └─────────┘                  │
                    ┌───────────┴────────────┐
                    │                        │
                 До 10                    10+
                    │                        │
              ┌─────▼──────┐         ┌──────▼─────────┐
              │  STARTER   │         │   POSTGRESQL   │
              │   $7/мес   │         │   + STARTER    │
              │  (SQLite)  │         │    $14/мес     │
              └────────────┘         └────────────────┘
```

---

## 🎓 Рекомендации

### Для разработчика / фрилансера:
- Начните с **Free** для демо
- Переходите на **Starter** когда нужна надёжность
- Стоимость: $7/мес ≈ 1 чашка кофе в кофейне

### Для стартапа / малого бизнеса:
- **Starter + SQLite** ($7/мес) - отличный старт
- Обновитесь до **PostgreSQL** ($14/мес) когда:
  - Больше 50 пользователей
  - Больше 1000 записей в БД
  - Нужны сложные отчёты

### Для корпоративного использования:
- Сразу **PostgreSQL + Standard** ($32/мес)
- Рассмотрите выделенный сервер если:
  - 100+ одновременных пользователей
  - Критически важные данные
  - SLA требования

---

## 💡 Советы по экономии

### 1. Frontend всегда Static Site (бесплатно)
Не используйте Docker для frontend - Static Site работает быстрее и бесплатно!

### 2. Один Backend на несколько проектов
Если у вас несколько небольших проектов, можно использовать один backend сервис.

### 3. Используйте Free tier для staging/dev
- Production: Starter/PostgreSQL
- Staging/Dev: Free tier

### 4. Оптимизируйте Docker образы
Меньше размер → быстрее deploy → меньше build minutes

---

## 📞 Поддержка

Если не уверены какой план выбрать:
- Начните с **Free** для теста
- Обновитесь когда поймёте требования
- Render позволяет легко менять планы

**Вопросы?**
- [Render Pricing](https://render.com/pricing)
- [Render Community](https://community.render.com)

---

Удачи с развертыванием! 🚀

