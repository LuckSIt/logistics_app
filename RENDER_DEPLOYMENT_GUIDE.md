# 🚀 Полное руководство по развертыванию Верес-Тариф на Render

## ⚠️ ВАЖНО: Ограничения Free Tier

**Render Free Tier НЕ поддерживает persistent storage (диски)!**

Это означает:
- ✅ Приложение будет работать
- ⚠️ **База данных будет ПЕРЕСОЗДАВАТЬСЯ при каждом деплое**
- ⚠️ Все данные (пользователи, тарифы) будут **УДАЛЕНЫ при обновлении**
- ⚠️ Сервисы **"засыпают"** после 15 минут неактивности

### Варианты решения:

**1. Free Tier для тестирования** (используйте `render.yaml`)
- Подходит для демонстрации функционала
- Данные не сохраняются между деплоями
- Бесплатно, но с ограничениями

**2. Starter Plan для продакшена** (используйте `render-paid.yaml`)
- $7/месяц за backend сервис
- Persistent disk для базы данных
- Сервис всегда активен (не засыпает)
- **Рекомендуется для реального использования**

**3. PostgreSQL + Starter Plan** (лучший вариант для продакшена)
- $7/месяц за backend + $7/месяц за PostgreSQL
- Полноценная реляционная БД
- Резервное копирование
- Масштабируемость

---

## 📋 Содержание
1. [Предварительные требования](#предварительные-требования)
2. [Подготовка проекта](#подготовка-проекта)
3. [Создание аккаунта на Render](#создание-аккаунта-на-render)
4. [Развертывание Backend](#развертывание-backend)
5. [Развертывание Frontend](#развертывание-frontend)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Проверка работоспособности](#проверка-работоспособности)
8. [Устранение неполадок](#устранение-неполадок)
9. [Обновление приложения](#обновление-приложения)

---

## 📌 Предварительные требования

### 1. Аккаунт GitHub
- Создайте аккаунт на [GitHub](https://github.com) (если еще нет)
- Убедитесь, что ваш проект загружен в GitHub репозиторий

### 2. Аккаунт Render
- Зарегистрируйтесь на [Render](https://render.com)
- Бесплатный план (Free Tier) подходит для начала

### 3. Убедитесь, что проект готов
```bash
# Проверьте, что все изменения закоммичены
git status

# Если есть незакоммиченные изменения
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

---

## 🔧 Подготовка проекта

### 1. Проверка структуры проекта

Убедитесь, что у вас есть следующие файлы:
```
logistics_app/
├── backend/
│   ├── main.py
│   ├── requirements.txt
│   ├── init_db.py
│   ├── update_db_schema.py
│   └── ... (остальные файлы)
├── frontend/
│   ├── package.json
│   ├── src/
│   └── public/
├── Dockerfile.backend
├── Dockerfile.frontend
├── render.yaml
├── .dockerignore
└── RENDER_DEPLOYMENT_GUIDE.md (этот файл)
```

### 2. Добавление health check endpoint в backend

Откройте `backend/main.py` и убедитесь, что есть health endpoint:

```python
@app.get("/health")
async def health_check():
    return {
        "status": "healthy",
        "timestamp": datetime.now().isoformat()
    }
```

Если его нет, добавьте этот код.

### 3. Обновление конфигурации CORS

В `backend/main.py` обновите CORS для работы с Render:

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",
        "https://*.onrender.com",  # Разрешаем Render домены
        "*"  # Для разработки (удалите в продакшене)
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 4. Коммит изменений

```bash
git add .
git commit -m "Add Render configuration and health check"
git push origin main
```

---

## 🌐 Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь через GitHub (рекомендуется)
4. Разрешите Render доступ к вашим репозиториям

---

## 🐍 Развертывание Backend

### Вариант 1: Автоматическое развертывание (через render.yaml)

1. **Создайте новый Blueprint**
   - В Render Dashboard нажмите **"New +"** → **"Blueprint"**
   - Подключите ваш GitHub репозиторий
   - Render автоматически найдет `render.yaml`
   - Нажмите **"Apply"**

### Вариант 2: Ручное развертывание (пошагово)

#### Шаг 1: Создание Web Service для Backend

1. В Render Dashboard нажмите **"New +"** → **"Web Service"**

2. **Подключите репозиторий:**
   - Выберите "Connect a repository"
   - Найдите ваш репозиторий `logistics_app`
   - Нажмите **"Connect"**

3. **Настройте сервис:**
   ```
   Name: veres-backend
   Region: Frankfurt (или ближайший к вам)
   Branch: main
   Runtime: Docker
   Dockerfile Path: ./Dockerfile.backend
   Docker Context: .
   ```

4. **Выберите план:**
   - Для начала выберите **"Free"**
   - Можно обновить позже при необходимости

5. **Настройте переменные окружения** (см. раздел ниже)

6. Нажмите **"Create Web Service"**

#### Шаг 2: Добавление Persistent Disk (ТОЛЬКО для платного плана!)

⚠️ **ВАЖНО:** Persistent Disks доступны только на платных планах (Starter и выше)!

**Для Free Tier:** Пропустите этот шаг. БД будет пересоздаваться при каждом деплое.

**Для Starter Plan ($7/месяц):**

1. Обновите план сервиса:
   - Backend Service → **"Settings"** → **"Instance Type"**
   - Выберите **"Starter"** ($7/месяц)
   - Подтвердите изменения

2. Добавьте диск:
   - Перейдите в **"Disks"**
   - Нажмите **"Add Disk"**
   - Настройте диск:
     ```
     Name: veres-data
     Mount Path: /app/data
     Size: 1 GB (можно увеличить при необходимости)
     ```
   - Сохраните изменения
   - Сервис автоматически перезапустится

---

## ⚛️ Развертывание Frontend

### Вариант 1: Static Site (Рекомендуется для Free tier)

1. **Создайте Static Site:**
   - В Render Dashboard: **"New +"** → **"Static Site"**
   - Подключите тот же репозиторий

2. **Настройте сборку:**
   ```
   Name: veres-frontend
   Branch: main
   Build Command: cd frontend && npm install && npm run build
   Publish Directory: frontend/dist
   ```

3. **Добавьте Rewrite Rules для React Router:**
   - В разделе **"Redirects/Rewrites"** добавьте:
   ```
   Source: /*
   Destination: /index.html
   Action: Rewrite
   ```

4. Нажмите **"Create Static Site"**

### Вариант 2: Docker Web Service

1. **Создайте Web Service для Frontend:**
   - **"New +"** → **"Web Service"**
   - Подключите репозиторий

2. **Настройте:**
   ```
   Name: veres-frontend
   Runtime: Docker
   Dockerfile Path: ./Dockerfile.frontend
   Docker Context: .
   ```

3. Создайте сервис

---

## 🔐 Настройка переменных окружения

### Backend Environment Variables

В настройках backend сервиса (**Settings** → **Environment**):

```bash
# Обязательные
PYTHONPATH=/app
PYTHONUNBUFFERED=1
PORT=8000

# Секретный ключ (сгенерируйте новый!)
SECRET_KEY=your-super-secret-key-change-this-in-production

# База данных
DATABASE_URL=sqlite:///./data/veres.db

# API ключи (если используете)
# OPENAI_API_KEY=your-openai-key
# HUGGINGFACE_TOKEN=your-hf-token

# Опциональные
DEBUG=False
LOG_LEVEL=INFO
```

**Генерация SECRET_KEY:**
```python
# Выполните в Python
import secrets
print(secrets.token_urlsafe(32))
```

### Frontend Environment Variables

Если используете Static Site, создайте файл `frontend/.env.production`:

```bash
VITE_API_URL=https://veres-backend.onrender.com
```

Замените `veres-backend` на имя вашего backend сервиса.

---

## 🔗 Настройка связи Frontend-Backend

### 1. Получите URL backend сервиса

После развертывания backend вы получите URL вида:
```
https://veres-backend.onrender.com
```

### 2. Обновите Frontend конфигурацию

**Вариант A: Через переменные окружения**

Создайте `frontend/.env.production`:
```bash
VITE_API_URL=https://veres-backend.onrender.com
```

Используйте в коде:
```javascript
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000';
```

**Вариант B: Hardcode (временно)**

В вашем frontend коде обновите API URL:
```javascript
// frontend/src/main.jsx или config файл
const API_BASE_URL = 'https://veres-backend.onrender.com';
```

### 3. Коммит и push

```bash
git add .
git commit -m "Update API URL for production"
git push origin main
```

Render автоматически пересоберет сервисы.

---

## ✅ Проверка работоспособности

### 1. Backend Health Check

Откройте в браузере:
```
https://veres-backend.onrender.com/health
```

Должны увидеть:
```json
{
  "status": "healthy",
  "timestamp": "2025-10-07T..."
}
```

### 2. API Documentation

```
https://veres-backend.onrender.com/docs
```

Должна открыться Swagger UI документация.

### 3. Frontend

Откройте URL вашего frontend сервиса:
```
https://veres-frontend.onrender.com
```

### 4. Проверка логов

В Render Dashboard:
- Откройте сервис
- Перейдите в **"Logs"**
- Проверьте наличие ошибок

---

## 🐛 Устранение неполадок

### Проблема: Backend не запускается

**Решение 1: Проверьте логи**
```bash
# В Render Dashboard → Backend Service → Logs
# Ищите строки с ERROR или CRITICAL
```

**Решение 2: Проверьте зависимости**
- Убедитесь, что `requirements.txt` содержит все необходимые пакеты
- Проверьте версии пакетов на совместимость

**Решение 3: Увеличьте timeout**
В `render.yaml` или через UI:
```yaml
healthCheckPath: /health
healthCheckTimeout: 60  # Увеличьте если нужно
```

### Проблема: Frontend не может подключиться к Backend

**Решение 1: CORS настройки**
В `backend/main.py`:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://veres-frontend.onrender.com"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**Решение 2: Проверьте API URL**
- Убедитесь, что frontend использует правильный backend URL
- Проверьте `.env.production` или hardcoded URL

### Проблема: База данных не сохраняется / данные теряются после деплоя

**Причина:** На Free Tier нет persistent storage.

**Решения:**

1. **Обновитесь до Starter Plan** ($7/месяц)
   - Backend Service → **"Settings"** → **"Instance Type"** → **"Starter"**
   - Затем добавьте Persistent Disk:
     - **"Disks"** → **"Add Disk"**
     - Mount Path: `/app/data`
     - Size: 1 GB
   - Перезапустите сервис

2. **Используйте внешнюю БД** (PostgreSQL)
   - Создайте PostgreSQL базу на Render ($7/месяц)
   - Обновите `DATABASE_URL` в environment variables
   - Измените код для работы с PostgreSQL вместо SQLite

3. **Примите ограничение Free Tier**
   - Используйте только для тестирования
   - Данные будут пересоздаваться при каждом обновлении
   - Создавайте тестового пользователя через `init_db.py` при каждом деплое

### Проблема: Free tier засыпает

Render Free tier:
- Засыпает после 15 минут неактивности
- Первый запрос может занять 30-60 секунд

**Решения:**
1. Обновитесь до платного плана ($7/месяц)
2. Используйте [UptimeRobot](https://uptimerobot.com) для ping каждые 5 минут
3. Используйте [Cron-job.org](https://cron-job.org) для периодических запросов

### Проблема: Большой размер Docker образа

**Решение: Multi-stage builds**
- Уже реализовано в `Dockerfile.frontend`
- Для backend можно добавить аналогично

### Проблема: Медленная сборка

**Решение: Кэширование слоев**
- Docker автоматически кэширует слои
- Убедитесь, что `requirements.txt` копируется до кода
- Используйте `.dockerignore`

---

## 🔄 Обновление приложения

### Автоматическое обновление (рекомендуется)

Render автоматически пересобирает сервис при push в GitHub:

```bash
# Внесите изменения в код
git add .
git commit -m "Update feature"
git push origin main

# Render автоматически:
# 1. Обнаружит изменения
# 2. Пересоберет Docker образ
# 3. Развернет новую версию
# 4. Переключит трафик без простоя
```

### Ручное обновление

1. Откройте сервис в Render Dashboard
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Или выберите конкретный commit/branch

### Откат к предыдущей версии

1. В Render Dashboard → **"Events"**
2. Найдите предыдущий успешный deploy
3. Нажмите **"Rollback to this deploy"**

---

## 💰 Стоимость на Render

### Free Tier (бесплатно)
- ✅ 750 часов в месяц для Web Services
- ✅ Unlimited Static Sites
- ✅ 100 GB bandwidth
- ⚠️ Сервисы засыпают после 15 мин неактивности
- ⚠️ Холодный старт ~30-60 секунд

### Starter ($7/месяц за сервис)
- ✅ Всегда активен (без засыпания)
- ✅ 400 build minutes
- ✅ Быстрый старт
- ✅ Custom domains

### Рекомендации:
- **Разработка/тестирование**: Free tier
- **Малый бизнес**: Starter для backend, Free для frontend
- **Продакшн**: Starter для обоих сервисов

---

## 📊 Мониторинг и аналитика

### Встроенный мониторинг Render

1. **Metrics:**
   - CPU usage
   - Memory usage
   - Request count
   - Response time

2. **Logs:**
   - Real-time логи
   - Поиск и фильтрация
   - Скачивание логов

3. **Events:**
   - История deployments
   - Status changes
   - Errors

### Дополнительный мониторинг

**UptimeRobot** (бесплатно):
```
Monitor Type: HTTP(s)
URL: https://veres-backend.onrender.com/health
Interval: 5 minutes
```

**Sentry** (для отслеживания ошибок):
```bash
# Добавьте в requirements.txt
sentry-sdk[fastapi]

# В main.py
import sentry_sdk
sentry_sdk.init(dsn="your-sentry-dsn")
```

---

## 🔒 Безопасность

### 1. Защита переменных окружения

- ✅ Используйте Environment Variables в Render
- ❌ Никогда не коммитьте `.env` файлы с секретами
- ✅ Генерируйте сложные `SECRET_KEY`

### 2. HTTPS

- ✅ Render автоматически предоставляет SSL сертификаты
- ✅ Все соединения зашифрованы

### 3. Custom Domain (опционально)

1. В Render Dashboard → Service → **"Settings"** → **"Custom Domain"**
2. Добавьте ваш домен (например, `app.veres-tariff.com`)
3. Настройте DNS:
   ```
   CNAME: app.veres-tariff.com → veres-frontend.onrender.com
   ```

### 4. Ограничение rate limiting

В `backend/main.py`:
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.get("/api/endpoint")
@limiter.limit("10/minute")
async def endpoint():
    return {"message": "Limited endpoint"}
```

---

## 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Status](https://status.render.com/)
- [Render Community](https://community.render.com/)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
- [React Deployment](https://vitejs.dev/guide/static-deploy.html)

---

## 🎯 Чеклист успешного развертывания

### Перед развертыванием:
- [ ] Код закоммичен и запушен в GitHub
- [ ] `Dockerfile.backend` и `Dockerfile.frontend` готовы
- [ ] `.dockerignore` настроен
- [ ] `render.yaml` создан
- [ ] Health endpoint добавлен в backend
- [ ] CORS настроен правильно

### После развертывания Backend:
- [ ] Backend сервис запущен (статус: Live)
- [ ] Health check работает: `/health`
- [ ] API docs доступны: `/docs`
- [ ] Persistent Disk добавлен для БД (опционально, только для Starter+)
- [ ] Environment variables настроены
- [ ] Логи не показывают критичных ошибок
- [ ] ⚠️ Если Free Tier: понимаю, что данные не сохраняются между деплоями

### После развертывания Frontend:
- [ ] Frontend сервис запущен
- [ ] Сайт открывается в браузере
- [ ] React Router работает (переходы между страницами)
- [ ] Frontend подключается к Backend API
- [ ] Статика загружается (CSS, JS, изображения)

### Финальная проверка:
- [ ] Можно зарегистрировать пользователя
- [ ] Можно войти в систему
- [ ] Основной функционал работает
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в backend логах

---

## 🆘 Получение помощи

Если возникли проблемы:

1. **Проверьте логи:**
   - Render Dashboard → Service → Logs
   - Найдите ERROR или WARNING сообщения

2. **Render Community:**
   - [community.render.com](https://community.render.com/)
   - Задайте вопрос с тегом вашего стека

3. **GitHub Issues:**
   - Создайте issue в вашем репозитории
   - Приложите логи и описание проблемы

4. **Документация:**
   - [Render Docs](https://render.com/docs)
   - [FastAPI Docs](https://fastapi.tiangolo.com/)

---

## 🎉 Поздравляем!

Ваше приложение **Верес-Тариф** теперь развернуто на Render и доступно в интернете!

**Следующие шаги:**
1. Настройте мониторинг
2. Добавьте custom domain (опционально)
3. Настройте резервное копирование БД
4. Оптимизируйте производительность
5. Масштабируйте при необходимости

Удачи! 🚀

