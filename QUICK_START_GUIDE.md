# 🚀 Быстрый Старт: OCR Сервис

## 📋 Что добавлено

В проект добавлен **готовый сервис для распознавания текста** и автоматического структурирования данных согласно существующим формам.

## ⚡ Быстрый тест

### 1. Запуск сервера
```bash
uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
```

### 2. Тестирование OCR
```bash
python test_ocr_service.py
```

### 3. Демонстрация возможностей
```bash
python demo_ocr_service.py
```

## 🔧 API Использование

### Извлечение текста из файла
```bash
curl -X POST "http://127.0.0.1:8000/enhanced-ocr/extract-text" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@document.pdf"
```

### Парсинг с сохранением в БД
```bash
curl -X POST "http://127.0.0.1:8000/enhanced-ocr/parse-structured" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@tariff.xlsx" \
  -F "transport_type=auto" \
  -F "supplier_id=1" \
  -F "save_to_db=true"
```

## 📊 Поддерживаемые форматы

- **Документы**: PDF, DOCX, XLSX, XLS
- **Изображения**: PNG, JPG, JPEG, BMP, TIFF
- **Типы транспорта**: auto, sea, air, rail, multimodal

## 🎯 Что извлекается

- ✅ Маршруты (откуда → куда)
- ✅ Цены (рубли, доллары, евро)
- ✅ Типы транспорта/контейнеров
- ✅ Сроки доставки
- ✅ Даты валидности
- ✅ Базисы поставки
- ✅ Дополнительные поля

## 🔗 Интеграция

Сервис полностью интегрирован с:
- ✅ Существующими формами проекта
- ✅ Моделями базы данных
- ✅ Системой аутентификации
- ✅ Шаблонами коммерческих предложений

## 📞 Готово к использованию!

Сервис готов к передаче заказчику и может быть использован в продакшене.
